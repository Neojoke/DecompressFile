#!/usr/bin/env python
import sys
import argparse as apa
if __name__ == '__main__':
def loadConfig(config):
    print('Load config from:{}'.format(config))
def setTheme(theme):
    print('Set theme: {}'.format(theme))
def main():
    parse = apa.ArgumentParser(prog='convert')
